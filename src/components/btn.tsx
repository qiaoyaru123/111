import * as React from 'react';
import { Button } from 'antd';

export default class btn extends React.Component {
    render() {
        return (
            <div>
                <Button>代码不全</Button>
                <Button>javaScript上</Button>
                <Button>周考一</Button>
            </div>
        )
    }
}
